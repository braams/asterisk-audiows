[audiows]
exten = 100,1,Answer()
same = n,AudioWS(ws://localhost:8080/syncecho)
same = n,Hangup()

exten = 200,1,Answer()
same = n,AudioWS(ws://localhost:8080/syncsin)
same = n,Hangup()

exten = 300,1,Answer()
same = n,AudioWS(ws://localhost:8080/asyncsin)
same = n,Hangup()

[from-phone]
include =>audiows